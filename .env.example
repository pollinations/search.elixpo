# ── LLM / AI ──────────────────────────────────────────────────────────────────
TOKEN=pollinations_ai or api provider token here
MODEL=model name here (e.g., gpt-3.5-turbo, gpt-4, kimi)
IMAGE_MODEL=image model name here (e.g., gemini-fast, or any other image model supported by your API provider)
HF_TOKEN=lixsearch HF token

# ── Redis ──────────────────────────────────────────────────────────────────────
REDIS_HOST=localhost
REDIS_PORT=9530
REDIS_URL=redis://localhost:9530/0

# ── Frontend dev server (search.elixpo) ───────────────────────────────────────
# Proxy target for gulp browser-sync – points to the running lixSearch backend
API_PROXY_TARGET=http://localhost:9002